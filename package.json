{
  "name": "coco-dental",
  "version": "1.0.0",
  "scripts": {
    "dev:next": "npm run dev --prefix ultravox-demo-template-vercel",
    "dev:nest": "npm run start:dev --prefix coco-dental-nest-app",
    "dev": "concurrently \"npm run dev:next\" \"npm run dev:nest\""
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
